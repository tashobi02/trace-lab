# Reproducibility

This project ensures reproducibility by relying on frozen model artifacts and explicit dependency versioning. Original historical evidence remains preserved, mapped via `migration_map.csv`.
