The corrected native source archive is the exact source used for the verified baseline. Verify every extracted file against `../source_manifest.json`. `reset_and_lifecycle.patch` records the corrections relative to the acquired source; `original_local_changes.patch` preserves acquisition-time changes. Do not apply the repair twice to the corrected archive. Runtime player copies are writable; frozen input files are checked before and after each stage.
