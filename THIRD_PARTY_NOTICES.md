# Third-party notices

Trace Lab integrates with the Affectively Framework and relies on Python, NumPy, PyTorch, Stable-Baselines3, scikit-learn, Gymnasium, ML-Agents and Project Jupyter components. Each dependency retains its own license.

This workspace commit records dependency identities and installation instructions. It does not distribute the Unity player, trained models, datasets, cached package archives or upstream source. Upstream notices must accompany any later source or asset inclusion. Trace Lab's root LICENSE applies to its original repository contributions.
