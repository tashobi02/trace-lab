Use each notebook's declared kernel. Start with setup and asset restoration, then `analysis/compare_baselines`, `analysis/export_valid_traces`, `analysis/inspect_training_progress`, followed by verification and inspection notebooks. Tests exercise executable modules; experiment notebooks default to explicit historical review. Library imports execute tagged definitions only. See `docs/notebook_architecture.md`.
